{
    "version": "2.0",
    "extensionBundle": {
      "id": "Microsoft.Azure.Functions.ExtensionBundle",
      "version": "[1.*, 2.0.0)"
    },
    "httpWorker": {
      "description": {
        "defaultExecutablePath": "${GOPATH}/bin/dlv",
        "defaultWorkerPath": "go/go-azure-action-server",
        "arguments": ["debug", "--listen=:40000", "--headless=true", "--api-version=2", "--log"]

      }
    }
  }